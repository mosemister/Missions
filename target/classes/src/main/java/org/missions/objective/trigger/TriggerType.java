package org.missions.objective.trigger;

import org.missions.objective.ObjectiveType;

/**
 * A TriggerType is a objective type that can only be complete when
 * the specified task has been complete
 */
public interface TriggerType extends ObjectiveType {


}
